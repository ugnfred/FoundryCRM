[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
markers =
    slow: mark test as slow-running
    smoke: mark as a quick smoke-test subset
log_cli = true
log_cli_level = WARNING
