# Copy to .env (same folder) and fill in your values.
# .env is gitignored — never commit your real keys.

# Anthropic (required for all LLM agents)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ── BA Flow Tester credentials ──────────────────────────────────────────────
# Deployed backend URL (Railway)
API_BASE_URL=https://foundrycrm-production.up.railway.app

# Supabase project (for auth — the tester logs in via the Supabase auth API)
SUPABASE_URL=https://zdpfbtvuxncwbvovfnxk.supabase.co
SUPABASE_ANON_KEY=sb_publishable_4-xDBqgwIgjtAHcLQH3T5Q_UCREt6ZI

# Test user credentials (must exist in Supabase Auth with admin role)
TEST_EMAIL=admin@foundryerp.test
TEST_PASSWORD=your_password_here
